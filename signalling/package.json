{
  "name": "@aicountly/remote-signalling",
  "version": "1.0.0",
  "private": true,
  "description": "WebRTC signalling relay for AICOUNTLY Remote. Verifies tokens minted by the CI4 API; holds no database and decides nothing.",
  "type": "module",
  "main": "src/server.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
